# 403 Accounts API 文档

本文档说明用于查询当前返回 403 且包含 Google Code Assist 认证跳转链接的账号列表接口。

---

## 1. 接口用途

该接口用于直接返回当前命中的 403 账号信息，并提取其对应认证 URL。

适用场景：

- 外部程序轮询当前需要重新认证的账号
- 批量获取失效账号的邮箱与认证链接
- 分开查看 `antigravity` 与 `geminicli` 的 403 账号

---

## 2. 接口地址

`GET /admin/oauth/403-accounts`

对应实现：

- 路由入口：`/admin/oauth/403-accounts`
- 实现文件：`src/routes/admin.js`

---

## 3. 鉴权方式

该接口要求传入管理员密码。

### Query 参数

| 参数       | 类型   | 必填 | 说明       |
| ---------- | ------ | ---: | ---------- |
| `password` | string |   是 | 管理员密码 |

### 调用示例

```http
GET /admin/oauth/403-accounts?password=your-admin-password
```

---

## 4. 筛选规则

接口会扫描当前账号列表，仅返回满足以下条件的记录：

1. 错误信息中包含 `403`
2. 错误信息中可以提取出目标为 `https://developers.google.com/gemini-code-assist/auth/` 的认证 URL

接口会自动兼容：

- 普通 URL
- 带转义斜杠的 URL
- URL 编码后的跳转地址
- Google 登录链接中的 `continue` 参数

---

## 5. 去重规则

- `antigravity` 与 `geminicli` 分开返回
- 聚合结果在 `accounts` 中统一输出
- 若同一邮箱同时存在于两边，优先保留 `antigravity`

---

## 6. 返回结构

接口当前仅返回你需要的最简字段：

- `email`
- `url`

### 返回示例

```json
{
  "success": true,
  "data": {
    "accounts": [
      {
        "email": "user1@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      },
      {
        "email": "user2@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ],
    "antigravity": [
      {
        "email": "user1@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ],
    "geminicli": [
      {
        "email": "user2@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ]
  }
}
```

### 字段说明

| 字段               | 说明                              |
| ------------------ | --------------------------------- |
| `data.accounts`    | 聚合后的最终结果                  |
| `data.antigravity` | antigravity 来源结果              |
| `data.geminicli`   | Gemini CLI 来源结果               |
| `email`            | 命中的账号邮箱                    |
| `url`              | 从 403 错误信息中提取出的认证链接 |

---

## 7. 失败示例

### 管理员密码错误

```json
{
  "success": false,
  "message": "密码验证失败"
}
```

---

## 8. 相关代码位置

- 403 API 路由：`src/routes/admin.js`
- 403 URL 提取逻辑：`src/routes/admin.js`
- 账号去重与分组逻辑：`src/routes/admin.js`
