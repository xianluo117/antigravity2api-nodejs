# 403 Accounts API 文档

本文档说明 [`GET /admin/oauth/403-accounts`](src/routes/admin.js:802) 的用途、鉴权方式、筛选规则以及邮箱查询用法。

---

## 1. 接口用途

该接口用于查询当前命中的 403 账号，并返回其对应的 Google Code Assist 认证 URL。

适用场景：

- 外部程序轮询当前需要重新认证的账号
- 批量获取失效账号的邮箱与认证链接
- 通过邮箱直接查询单个账号对应的认证 URL
- 分开查看 `antigravity` 与 `geminicli` 的 403 账号

---

## 2. 接口地址

`GET /admin/oauth/403-accounts`

对应实现：

- 路由入口：[`GET /admin/oauth/403-accounts`](src/routes/admin.js:802)
- 实现文件：[src/routes/admin.js](src/routes/admin.js)

---

## 3. 鉴权方式

该接口要求传入管理员密码。

### Query 参数

| 参数       | 类型   | 必填 | 说明                                     |
| ---------- | ------ | ---: | ---------------------------------------- |
| `password` | string |   是 | 管理员密码                               |
| `email`    | string |   否 | 指定邮箱后，直接返回该邮箱对应的认证 URL |

### 调用示例

#### 3.1 查询全部 403 账号

```http
GET /admin/oauth/403-accounts?password=your-admin-password
```

#### 3.2 按邮箱查询对应 URL

```http
GET /admin/oauth/403-accounts?password=your-admin-password&email=user@example.com
```

---

## 4. 筛选规则

接口会扫描当前账号列表，仅返回满足以下条件的记录：

1. 错误信息中包含 `403`
2. 错误信息中可以提取出目标为 `https://developers.google.com/gemini-code-assist/auth/` 的认证 URL

接口会自动兼容：

- 普通 URL
- 带转义斜杠的 URL
- URL 编码后的跳转地址
- Google 登录链接中的 `continue` 参数

---

## 5. 去重规则

- `antigravity` 与 `geminicli` 分开返回
- 聚合结果在 `accounts` 中统一输出
- 若同一邮箱同时存在于两边，优先保留 `antigravity`

---

## 6. 返回结构

接口当前仅返回你需要的最简字段：

- `email`
- `url`

### 6.1 查询全部时的返回示例

```json
{
  "success": true,
  "data": {
    "accounts": [
      {
        "email": "user1@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      },
      {
        "email": "user2@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ],
    "antigravity": [
      {
        "email": "user1@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ],
    "geminicli": [
      {
        "email": "user2@example.com",
        "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
      }
    ]
  }
}
```

### 字段说明

| 字段               | 说明                              |
| ------------------ | --------------------------------- |
| `data.accounts`    | 聚合后的最终结果                  |
| `data.antigravity` | antigravity 来源结果              |
| `data.geminicli`   | Gemini CLI 来源结果               |
| `email`            | 命中的账号邮箱                    |
| `url`              | 从 403 错误信息中提取出的认证链接 |

### 6.2 按邮箱查询时的返回示例

```json
{
  "success": true,
  "data": {
    "email": "user1@example.com",
    "url": "https://accounts.google.com/signin/continue?sarp=1&scc=1&continue=https://developers.google.com/gemini-code-assist/auth/auth_success_gemini&flowName=GlifWebSignIn"
  }
}
```

---

## 7. 失败示例

### 7.1 管理员密码错误

```json
{
  "success": false,
  "message": "密码验证失败"
}
```

### 7.2 邮箱未命中

当传入 [`email`](403api.md) 但未查询到对应记录时，接口返回：

```json
{
  "success": false,
  "message": "未找到该邮箱对应的认证URL"
}
```

---

## 8. 相关代码位置

- 403 API 路由：[router.get("/oauth/403-accounts")](src/routes/admin.js:802)
- 403 URL 提取逻辑：[extractForbiddenAuthUrl()](src/routes/admin.js:109)
- 账号去重与分组逻辑：[buildForbiddenGoogleAccountList()](src/routes/admin.js:145)
- 邮箱查询逻辑：[router.get("/oauth/403-accounts")](src/routes/admin.js:802)
