# 已完成的兼并

## 1. 本次完成项

### 1.1 上游 `baseURL Fallback` 首轮落地

本轮已将 upstream `aa2add8` 中与上游候选域名回退有关的核心思路，按当前本地代码结构吸收到 [`src/api/client.js`](src/api/client.js) 与 [`src/config/config.js`](src/config/config.js)。

目标不是直接照搬 upstream，而是在**保留本地现有增强逻辑**的前提下，实现：

- 主请求失败时，按上游候选域名顺序自动切换
- 避免对 `403` / `407` / `429` 这类不应切换域名的错误误触发 fallback
- 流式输出已经开始后，禁止 fallback，避免客户端收到脏流数据
- 模型列表、非流式、图片、轨迹、指标、checkpoint 等请求统一接入 fallback 包装层

---

## 2. 实际改动文件

### 2.1 [`src/api/client.js`](src/api/client.js)

已新增或调整以下能力：

#### a) 导入 [`getUpstreamConfig()`](src/config/config.js:661)
用于读取已接入的 [`upstream.json`](src/config/upstream.json)，从而构造候选上游配置。

#### b) 扩展 [`buildHeaders()`](src/api/client.js:192)
从固定 `Host` 改为支持 `hostOverride`。

作用：
- 在切换候选上游时，`Host` 头能随候选域名同步变化
- 避免请求 URL 已切换，但 `Host` 仍停留在默认域名

#### c) 新增候选组装函数
- [`getUpstreamApiCandidates()`](src/api/client.js:203)

作用：
- 从当前运行配置 [`config.api`](src/config/config.js:475) 与 [`upstream.json`](src/config/upstream.json) 读取候选域名链
- 以“当前实际使用配置”作为第一候选
- 再拼接 [`config.api.upstreamCandidates`](src/config/config.js:402) 指向的 tracked upstream 候选
- 自动去重，避免重复尝试同一目标

#### d) 新增错误判定函数
- [`shouldFallback()`](src/api/client.js:244)

当前策略：
- 不 fallback：`429`、`403`、`400`、`407`
- 允许 fallback：`503`、`5xx`、网络错误、超时错误
- 若错误对象带有 [`_skipFallback`](src/api/client.js:245) 标志，则强制禁止 fallback

这与本地现有逻辑兼容点在于：
- `429` 后续仍应交给第二优先级的三档冷却策略处理
- `403` 属于权限 / token 问题，切换域名通常无意义
- `407` 属于代理认证失败，本地已有代理池禁用逻辑，不能被 fallback 吞掉

#### e) 新增统一包装器
- [`withUpstreamFallback()`](src/api/client.js:272)

作用：
- 按候选顺序执行请求
- 失败后根据 [`shouldFallback()`](src/api/client.js:244) 决定是否切到下一个候选
- 全部失败时抛出最后一个错误
- 输出 fallback 日志，便于后续观察

#### f) 流式接口接入 fallback
- [`generateAssistantResponse()`](src/api/client.js:341)

关键处理：
- 对流式请求加入 fallback 封装
- 增加 `safeCallback` 和 `hasEmittedData`
- 一旦已向客户端输出流数据，就给异常打上 `_skipFallback`，从而阻止切换域名

这是这次改造里最关键的安全措施，因为流式场景不能在半路切到另一个 upstream，否则会导致客户端收到混杂数据。

#### g) 非流式 / 模型 / 图片 / 埋点接入 fallback
以下函数均已切换到 fallback 包装层：

- [`fetchRawModels()`](src/api/client.js:428)
- [`getAvailableModels()`](src/api/client.js:448)
- [`getModelsWithQuotas()`](src/api/client.js:525)
- [`generateAssistantResponseNoStream()`](src/api/client.js:542)
- [`generateImageForSD()`](src/api/client.js:659)
- [`sendRecordTrajectoryAnalytics()`](src/api/client.js:714)
- [`sendRecordCodeAssistMetrics()`](src/api/client.js:794)
- [`sendCheckPoint()`](src/api/client.js:875)

说明：
- [`sendLog()`](src/api/client.js:751) 未纳入 fallback，原因是它固定请求 `play.googleapis.com`，不属于 upstream 候选域名体系
- [`sendClientRegister()`](src/api/client.js:819)、[`sendClientFeature()`](src/api/client.js:839)、[`sendFrontEnd()`](src/api/client.js:855) 暂未纳入本轮 fallback，因为它们走的是 [`config.api.unleash`](src/config/config.js:400) 链路，不属于本次主 API baseURL fallback 最小闭环范围

---

### 2.2 [`src/config/config.js`](src/config/config.js)

已新增：
- [`getUpstreamConfig()`](src/config/config.js:661)

作用：
- 向 [`src/api/client.js`](src/api/client.js) 暴露已解析的 [`upstream.json`](src/config/upstream.json)
- 使 fallback 逻辑可以在运行时获取 tracked upstream 候选，而不是在 [`src/api/client.js`](src/api/client.js) 中重复读文件

---

## 3. 本轮自检结果

### 3.1 语法检查
已对以下文件执行语法检查并通过：

- [`src/api/client.js`](src/api/client.js)
- [`src/config/config.js`](src/config/config.js)

### 3.2 代码路径检查
已确认 [`src/api/client.js`](src/api/client.js) 中原本直接依赖以下地址的主调用点，已改为经过 fallback 包装层：

- [`config.api.url`](src/config/config.js:408)
- [`config.api.noStreamUrl`](src/config/config.js:410)
- [`config.api.modelsUrl`](src/config/config.js:409)
- [`config.api.recordTrajectory`](src/config/config.js:411)
- [`config.api.recordCodeAssistMetrics`](src/config/config.js:413)

### 3.3 风险控制
本轮刻意没有做的内容：

- 没有把 `429` 纳入 fallback，避免和后续三档冷却策略耦合混乱
- 没有直接重写 [`src/server/stream.js`](src/server/stream.js)
- 没有把前端积分 / 订阅 / UI 相关逻辑混进本轮改造
- 没有动 [`src/routes/admin.js`](src/routes/admin.js) 这类本地重改最重的文件

因此这次改动仍然属于**第二优先级中风险较低的一步**。

---

## 4. 与 upstream 思路的对应关系

参考 upstream 提交：
- [`aa2add8`](兼容合并上游方案.md:725)

本地吸收方式：
- 只学习 upstream 的“候选域名轮换 + 错误分类 + Host 同步”思想
- 不直接覆盖本地 [`src/api/client.js`](src/api/client.js)
- 不强行引入 upstream 的全部联动改动

这符合当前仓库“以 upstream 为参考进行本地优化，而不是生硬 merge”的总原则。

---

## 5. 当前状态

### 已完成
- [`upstream.json`](src/config/upstream.json) 接入
- TLS / h2 / 解压增强
- 模型列表更新
- `Transfer-Encoding` 请求头补充
- 上游 `baseURL Fallback` 首轮落地

### 下一步建议
继续处理第二优先级中的：
- [`三档冷却策略`](兼容合并上游方案.md:360)

原因：
- 它与本轮 fallback 已经形成自然衔接
- 当前 `429` 仍然明确保留给该策略处理
- 继续向上游的“错误分流与稳定性增强”主线推进，比先做 CSS 或积分功能更合理

---

## 6. 第二优先级补充完成项

### 6.1 [`三档冷却策略`](兼容合并上游方案.md:360) 完整落地

本轮已将 upstream 在 `429` 处理上的三段式思路，按当前本地代码结构吸收到 [`src/server/stream.js`](src/server/stream.js)、[`src/config/config.js`](src/config/config.js)、[`src/constants/index.js`](src/constants/index.js)、[`src/utils/tokenQuotaHelper.js`](src/utils/tokenQuotaHelper.js)、[`src/server/handlers/openai.js`](src/server/handlers/openai.js)、[`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)、[`src/server/handlers/claude.js`](src/server/handlers/claude.js)。

本次目标不是直接覆盖本地重试体系，而是在保留现有：
- [`with429Retry()`](src/server/stream.js:333)
- [`tokenCooldownManager`](src/auth/token_cooldown_manager.js:1)
- [`quotaManager`](src/auth/quota_manager.js:1)
- 三个主 handler 自有响应拼装逻辑

的前提下，把 upstream 的“长冷却 / 短冷却 / 瞬时重试 + 积分切换”安全嵌入本地代码。

---

## 7. 本轮实际改动文件

### 7.1 [`src/constants/index.js`](src/constants/index.js)

新增：
- [`SHORT_COOLDOWN_THRESHOLD`](src/constants/index.js)

作用：
- 定义短冷却阈值（5 秒）
- 与已有的 [`LONG_COOLDOWN_THRESHOLD`](src/constants/index.js:104) 共同构成三段式判断边界

当前三段划分为：
- 长冷却：`>= 1h`
- 短冷却：`>= 5s 且 < 1h`
- 瞬时重试：`< 5s` 或无明确延迟

### 7.2 [`src/config/config.js`](src/config/config.js)

新增配置接入：
- [`quota.longCooldownThreshold`](src/config/config.js:499)
- [`quota.shortCooldownThreshold`](src/config/config.js:505)
- [`alwaysUseCredits`](src/config/config.js:512)

作用：
- 允许通过本地配置覆盖长冷却 / 短冷却阈值
- 允许通过配置强制启用积分模式，而不必等待首轮 `429`

这一步的意义是把策略从“写死在代码里”提升为“运行时可调”。

### 7.3 [`src/utils/tokenQuotaHelper.js`](src/utils/tokenQuotaHelper.js)

新增并扩展：
- [`hasOtherAvailableModelGroups()`](src/utils/tokenQuotaHelper.js:16)
- [`getAvailableModelGroups()`](src/utils/tokenQuotaHelper.js:31)

并且加入了按分组模式区分核心模型组：
- `default`
- `geminicli`

作用：
- 当长冷却触发时，判断当前 token 是否还有其他核心模型组可用
- 如果还有可用组，则仅冻结当前模型组
- 如果所有核心模型组都不可用，则配合 handler 侧的 `markQuotaExhausted` 回调，把整个 token 标记为额度耗尽

### 7.4 [`src/server/stream.js`](src/server/stream.js)

本轮核心修改落在 [`with429Retry()`](src/server/stream.js:333)。

已新增能力：

#### a) 支持 `shouldUseCredits`
- 现在 [`with429Retry()`](src/server/stream.js:333) 会向业务回调传递 `(attempt, shouldUseCredits)`
- 首次命中适合瞬时重试的 `429` 时，会优先切到 `GOOGLE_ONE_AI` 积分再试一次

#### b) 长冷却分支
- 条件：`429` 且恢复时间 `>= longCooldownThreshold`
- 行为：
  - 解析上游恢复时间
  - 冻结当前 token 的当前模型组
  - 调用 [`hasOtherAvailableModelGroups()`](src/utils/tokenQuotaHelper.js:16) 判断是否还有其他核心模型组可用
  - 若无可用组，触发 handler 传入的 `markQuotaExhausted`

#### c) 短冷却分支
- 条件：`429` 且恢复时间 `>= shortCooldownThreshold` 且 `< longCooldownThreshold`
- 行为：
  - 仅冻结当前 token 的当前模型组
  - 直接终止本次请求内重试
  - 交给上层 token 轮换在下一次选择其他 token

#### d) 瞬时重试分支
- 条件：恢复时间很短，或未给出明确恢复时间
- 行为：
  - 第一次优先尝试积分模式
  - 后续仍按本地退避逻辑继续重试
  - 保留 jitter/backoff 思路，不破坏本地原有弹性退避行为

#### e) 日志语义增强
新增了清晰的日志分类：
- `[长冷却]`
- `[短冷却]`
- `[瞬时重试]`

这使后续排查时能更快定位当前 `429` 被判到了哪个档位。

### 7.5 [`src/server/handlers/openai.js`](src/server/handlers/openai.js)

已新增：
- [`applyCreditTypes()`](src/server/handlers/openai.js:77)
- `markQuotaExhausted` 回调传入 [`with429Retry()`](src/server/stream.js:333)

并把以下调用点全部改为可接收 `shouldUseCredits`：
- 流式图片
- 流式对话
- 假非流
- 非流式

核心效果：
- 当 [`with429Retry()`](src/server/stream.js:333) 判定要走积分模式时，[`generateAssistantResponse()`](src/api/client.js:341) 与 [`generateAssistantResponseNoStream()`](src/api/client.js:542) 收到的请求体会自动注入：
  - `enabledCreditTypes: ["GOOGLE_ONE_AI"]`

### 7.6 [`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)

与 [`openai`](src/server/handlers/openai.js:42) 同步完成：
- [`applyCreditTypes()`](src/server/handlers/gemini.js:170)
- `markQuotaExhausted`
- 流式 / 假非流 / 非流式 / 图片路径的积分切换接入

这保证了 Gemini 协议侧不会落后于 OpenAI 协议侧，避免同一底层 token 策略在不同入口行为不一致。

### 7.7 [`src/server/handlers/claude.js`](src/server/handlers/claude.js)

与另外两个 handler 同步完成：
- [`applyCreditTypes()`](src/server/handlers/claude.js:89)
- `markQuotaExhausted`
- 流式文本 / 流式图片 / 假非流 / 非流式 全路径接入积分切换

由于 Claude 响应拼装更复杂，这一步的重点是：
- 不破坏已有 thinking / tool_use / text block 事件顺序
- 仅替换发往底层生成函数的请求体
- 保持上层流式协议兼容

---

## 8. 本轮自检结果

### 8.1 语法检查
已通过 [`node --check`](package.json) 的文件包括：
- [`src/server/stream.js`](src/server/stream.js)
- [`src/config/config.js`](src/config/config.js)
- [`src/constants/index.js`](src/constants/index.js)
- [`src/utils/tokenQuotaHelper.js`](src/utils/tokenQuotaHelper.js)
- [`src/server/handlers/openai.js`](src/server/handlers/openai.js)
- [`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)
- [`src/server/handlers/claude.js`](src/server/handlers/claude.js)

### 8.2 结构一致性检查
已确认：
- 三个主协议 handler 的重试入口全部已接入 `shouldUseCredits`
- [`with429Retry()`](src/server/stream.js:333) 的新参数与现有调用方式兼容
- 当前未触碰 [`src/routes/admin.js`](src/routes/admin.js) 与前端界面逻辑
- 当前未触碰 [`/Backup`](/Backup) 与 [`/TEST`](/TEST)

### 8.3 风险边界
本轮刻意没有做的内容：
- 没有改动前端积分/订阅显示
- 没有把这套策略强行套到 [`geminicli`](src/server/handlers/geminicli.js) 主链路
- 没有重写 [`token_manager`](src/auth/token_manager.js) 架构，只复用已有 [`markQuotaExhausted()`](src/auth/token_manager.js:649)

因此这次仍然是“在现有重试系统上增量增强”，而不是新的架构重构。

---

## 9. 与 upstream 思路的对应关系

参考 upstream：
- [`4.3 三档冷却策略`](兼容合并上游方案.md:360)
- [`aa2add8`](兼容合并上游方案.md:725)

本地吸收方式：
- 学习 upstream 的三段式决策与积分切换思想
- 保留本地已有的 handler 封装与响应格式
- 复用本地现有的 cooldown / quota / token 轮换基础设施

这样可以在不破坏你现有大量本地增强的前提下，把 upstream 里真正有价值的稳定性逻辑吸收进来。

---

## 10. 当前状态更新

### 已完成
- [`upstream.json`](src/config/upstream.json) 接入
- TLS / h2 / 解压增强
- 模型列表更新
- `Transfer-Encoding` 请求头补充
- 上游 `baseURL Fallback` 首轮落地
- [`三档冷却策略`](兼容合并上游方案.md:360) 完整落地

### 下一步建议
继续处理第二优先级剩余项时，建议顺序为：
1. ~~[`CSS 拆分`](兼容合并上游方案.md:197)~~ ✅ 已完成
2. [`积分/订阅功能`](兼容合并上游方案.md:103)

原因：
- 当前"稳定性主线"已经完成两块核心能力：Fallback + 三档冷却
- 接下来再做稳定性收益已经没有前两项那么高
- ~~先做 [`CSS 拆分`](兼容合并上游方案.md:197) 风险相对可控~~ ✅ 已完成
- [`积分/订阅功能`](兼容合并上游方案.md:103) 涉及 [`src/routes/admin.js`](src/routes/admin.js)、[`public/index.html`](public/index.html)、[`public/js/config.js`](public/js/config.js) 等重改区域，应放在更后面处理

---

## 10. 第三轮完成项

### 10.1 [`CSS 模块化拆分`](兼容合并上游方案.md:197) 完整落地

**完成时间**: 2026-04-20

**参考 upstream**: 提交 [`1c7eb7e`](兼容合并上游方案.md:585) — *refactor: 切割css文件，分为数个模块*

**upstream 方案**: 拆为 5 个模块（base / components / layout / pages / responsive）

**本地方案**: 考虑到本地 [`public/style.css`](public/style.css) 已扩展到 4102 行，远超 upstream 初始规模，采用**更细粒度的 11 模块方案**，按职责边界拆分：

---

## 11. CSS 拆分改动详情

### 11.1 模块文件清单

| # | 文件 | 行数 | 职责 |
|---|------|------|------|
| 1 | [`css/variables.css`](public/css/variables.css) | 26 | CSS 变量定义（亮色/暗色） |
| 2 | [`css/base.css`](public/css/base.css) | 124 | Reset / 滚动条 / 字体 / body |
| 3 | [`css/layout.css`](public/css/layout.css) | 282 | 容器 / 登录 / 主内容 / Header / Tabs / 页面切换 |
| 4 | [`css/components.css`](public/css/components.css) | 235 | top-bar / stats / action-btns / bulk-actions / btn 系列 |
| 5 | [`css/tokens.css`](public/css/tokens.css) | 590 | Token 网格 / 卡片 / 状态 / 403错误 / 空状态 |
| 6 | [`css/settings.css`](public/css/settings.css) | 326 | 设置页面布局 / 导航 / 配置表单 / 轮询状态 |
| 7 | [`css/overlays.css`](public/css/overlays.css) | 359 | Toast / Modal / OAuth弹窗 / Loading遮罩 |
| 8 | [`css/controls.css`](public/css/controls.css) | 266 | 帮助提示 / 恢复默认 / 解锁按钮 / Textarea锁 / 开关 |
| 9 | [`css/quota.css`](public/css/quota.css) | 497 | 额度弹窗 / 标签页 / 网格 / Token内嵌额度 / `.hidden` |
| 10 | [`css/logs.css`](public/css/logs.css) | 566 | 日志页面 / 统计 / 搜索 / 筛选 / 条目 / 复制按钮 |
| 11 | [`css/pages-extra.css`](public/css/pages-extra.css) | 836 | 响应式 768px/480px / 导入拖拽 / IP封禁 / 白名单 |

**合计**: 4107 行（原 4102 行 + 5 行模块注释头）

### 11.2 聚合入口

[`public/style.css`](public/style.css) 改写为：

```css
@import url('css/variables.css');
@import url('css/base.css');
@import url('css/layout.css');
@import url('css/components.css');
@import url('css/tokens.css');
@import url('css/settings.css');
@import url('css/overlays.css');
@import url('css/controls.css');
@import url('css/quota.css');
@import url('css/logs.css');
@import url('css/pages-extra.css');
```

### 11.3 关键路径修正

- [`css/base.css`](public/css/base.css:17) 中背景图片路径从 `url("assets/bg.jpg")` 修正为 `url("../assets/bg.jpg")`（因文件从 `public/` 移到 `public/css/`）

### 11.4 兼容性保障

- [`public/index.html`](public/index.html:215) 的加载方式 `style.css?v=...` **无需改动**
- 内联 `<style>` 中的关键 Tab/Auth 样式保持不变（不依赖外部 CSS 文件）
- `@import` 在现代浏览器中级联加载，与原单文件行为等价

---

## 12. 自检结果

### 12.1 行数验证

```
variables.css    26
base.css        124
layout.css      282
components.css  235
tokens.css      590
settings.css    326
overlays.css    359
controls.css    266
quota.css       497
logs.css        566
pages-extra.css 836
─────────────────
TOTAL          4107  ✅ (原 4102 + 5 行注释头)
```

### 12.2 `url()` 路径检查

仅 [`css/base.css:17`](public/css/base.css:17) 一处使用 `url("../assets/bg.jpg")`，路径正确 ✅

---

## 13. 与 upstream 思路的对应关系

| upstream 做法 | 本地做法 | 差异原因 |
|--------------|---------|---------|
| 拆为 5 个模块 | 拆为 11 个模块 | 本地代码量是 upstream 的 2-3 倍，需要更细粒度 |
| `pages.css` 2349 行 | 按页面拆为 tokens/settings/logs/quota/pages-extra | 单个 pages.css 仍会过大 |
| 无 controls 模块 | 独立 controls.css | 开关/帮助提示/解锁按钮是本地增强功能 |
| 无 overlays 模块 | 独立 overlays.css | OAuth弹窗/Loading 是本地独有 |

---

### 已完成

- ✅ upstream.json 配置接入
- ✅ TLS 二进制 + h2 配置
- ✅ 模型列表更新
- ✅ Transfer-Encoding 请求头
- ✅ baseURL Fallback
- ✅ 三档冷却策略
- ✅ CSS 模块化拆分

### 下一步建议

继续处理第二优先级最后一项：
1. [`积分/订阅功能`](兼容合并上游方案.md:103) — 涉及前端 UI + 后端路由 + Token 管理器联动
