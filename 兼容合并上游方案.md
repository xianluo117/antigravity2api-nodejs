# 参考上游提交进行代码优化方案

> 生成时间: 2026-04-19  
> 当前分支 HEAD: `0139f7a` (66 commits ahead of merge-base)  
> upstream/main HEAD: `47820ad` (23 commits ahead of merge-base)  
> merge-base: `c982fdee`

---

## 一、上游 23 个提交功能分组详解

### 组 A：请求器架构重构 `aa29d82`

**目标**: 将请求器初始化从 `src/api/client.js` 中抽离，形成独立的 `RequesterManager` 单例。

**upstream 实现**:
- 新建 [`src/utils/requesterManager.js`](src/utils/requesterManager.js) (307 行)
- 从 `client.js` 删除了 ~30 行的内联请求器初始化代码
- `client.js` 改为 `import requesterManager from '../utils/requesterManager.js'`
- 删除 `client.js` 中的 `path`/`fileURLToPath` 导入
- 统一用 `requesterManager.request()` 替代原来的 `httpRequest`/`httpStreamRequest`
- `geminiTransport.js` 中 `runAxiosSseStream`/`runNativeSseStream` 合并为 `runSseStream`
- 支持热重载: `configReloader.js` 中调用 `requesterManager.reload()`
- 二进制探测: spawn 检测 TLS 二进制是否可执行，失败自动降级 axios

**本地状态**: 你在 `75bad3b` 已经做了类似重构 ("refactor: unify requester initialization and transport")，本地 `requesterManager.js` 有 350 行。

**优化建议**:
- ⚠️ **add/add 冲突核心** — 两边都新建了同名文件
- 对比两个版本的 `RequesterManager` 类：
  - upstream 版本: 异步初始化 (`_ensureInit()` 返回 Promise)，有 `_probeBinary()` 探测
  - 本地版本: 同步签名检测 (`_getConfigSignature()`)，有 `_resetState()` 和 `close()` 方法
- **建议**: 保留本地版本的签名检测机制（更精细），但参考 upstream 添加 `_probeBinary()` 二进制探测逻辑

---

### 组 B：Token 管理器解耦 `92e44ba` + `2d4071b` + `252a0bb` + `24a8f99`

**目标**: 将巨型 `token_manager.js` 拆分为多个职责单一的模块。

**upstream 新增模块**:

| 模块 | 行数 | 职责 |
|------|------|------|
| `src/auth/token_pool.js` | 383 | token 池管理（增删查、启用/禁用状态） |
| `src/auth/token_lifecycle_manager.js` | 184 | token 生命周期（刷新、过期检测） |
| `src/auth/token_rotation_strategy.js` | 250 | 轮询策略（round_robin/quota_exhausted/request_count） |
| `src/auth/token_validator.js` | 245 | token 验证（OAuth 验证、启用前校验） |
| `src/auth/project_id_fetcher.js` | 414 | project_id 获取 + 订阅/积分信息同步 |

**upstream `token_manager.js` 重构后的结构**:
```javascript
class TokenManager {
  constructor(filePath) {
    this.store = new TokenStore(filePath);
    this.pool = new TokenPool(this.store);
    this.lifecycle = new TokenLifecycleManager(this.store);
    this.projectIdFetcher = new ProjectIdFetcher();
    this.validator = new TokenValidator(this.store);
    this.strategy = null; // StrategyFactory.create(...)
  }
}
```

**本地状态**: 你在 `token_manager.js` 上有 11 个提交（+1230 行），主要是：
- 轮询策略优化（额度感知、随机起始凭证）
- 预热额度、显示当前凭证 ID
- OAuth 验证、project_id 恢复
- 启动错误记录

**优化建议**:
- 🔑 **这是最大的架构差异** — upstream 做了解耦，你在原架构上做了功能增强
- **推荐方案**: 参考 upstream 的拆分思路，将你的功能增强迁移到新模块中：
  1. 将你的轮询策略优化代码迁移到独立的 `token_rotation_strategy.js`
  2. 将 OAuth 验证逻辑迁移到 `token_validator.js`
  3. 将 project_id 相关逻辑迁移到 `project_id_fetcher.js`
  4. 保留你的额度感知、预热等增强功能
- **不建议直接采纳 upstream 版本**，因为你的功能增强更丰富，但应参考其拆分架构

---

### 组 C：Token 禁用逻辑重构 `2d4071b`

**目标**: 在 `with429Retry` 中实现三档冷却策略 + 配额耗尽级联检测。

**upstream 实现**:
- 新建 [`src/utils/tokenQuotaHelper.js`](src/utils/tokenQuotaHelper.js) (37 行) — `hasOtherAvailableModelGroups()` / `getAvailableModelGroups()`
- `src/server/stream.js` 中 `with429Retry`:
  - 新增 `shouldUseCredits` 参数传递给回调
  - 三档冷却: 长冷却(≥1h 禁用模型组) → 短冷却(≥5s 冻结 token+model) → 即时重试
  - 所有模型组禁用时标记整个 token 配额耗尽 (`markTokenQuotaExhausted`)
- handlers (`claude.js`/`gemini.js`/`openai.js`) 的回调签名变为 `(attempt, shouldUseCredits) => {}`

**本地状态**: `src/server/stream.js` 只有 1 个提交 (`ab1689d` geminicli quota grouping)，改动较小。

**优化建议**:
- ✅ **直接采纳** upstream 的三档冷却策略 — 这是纯功能增强，本地改动少
- 需要同步修改 handlers 的回调签名
- 新建 `src/utils/tokenQuotaHelper.js`

---

### 组 D：积分/订阅功能 `906eb00` + `3349732` + `0da6c36` + `271d630` + `f78f1f1` + `d6c9e8d` + `b332693` + `2e9101c`

**目标**: 支持 Google One AI 积分使用，前端显示订阅和积分信息。

**upstream 实现**:
- `src/api/client.js`: 添加 `enabledCreditTypes: ["GOOGLE_ONE_AI"]` 到请求体
- `src/server/handlers/claude.js` (+265 行重写): 回调中根据 `shouldUseCredits` 动态注入积分字段
- `src/server/handlers/gemini.js` / `openai.js`: 同上
- `src/auth/project_id_fetcher.js`: 获取订阅信息 + 积分余额
- `src/config/config.js`: 新增 `alwaysUseCredits` 配置项
- `public/js/tokens.js`: 前端显示订阅和积分
- `public/js/config.js`: 前端积分策略开关
- `public/index.html`: 积分配置 UI

**本地状态**: `src/server/handlers/claude.js` 只有 1 个小改动 (27 行)。

**优化建议**:
- ✅ **建议采纳** — 积分功能是纯新增，本地几乎没有冲突
- 实施步骤:
  1. 在 `project_id_fetcher.js` 中添加订阅/积分获取逻辑
  2. 修改三个 handler 的 `with429Retry` 回调支持 `shouldUseCredits`
  3. 添加 `config.alwaysUseCredits` 配置
  4. 更新前端 UI

---

### 组 E：上游 baseURL Fallback + upstream.json `aa2add8`

**目标**: 将上游端点配置从 config.json 分离到独立的 `upstream.json`（git 跟踪），并实现多端点 fallback。

**upstream 实现**:
- 新建 [`src/config/upstream.json`](src/config/upstream.json) (54 行): 定义 production/daily/sandbox/sukaka 四个端点
- `src/config/config.js`:
  - 新增 `getConfigPaths()` 返回 `upstreamJsonPath`
  - 加载 upstream.json 作为只读配置
  - 自动清理 config.json 中的旧上游字段
  - `getActiveApiConfig(jsonConfig, upstreamCfg)` 双参数
  - 新增 `upstreamCandidates` 配置: `["production", "sandbox", "daily", "sukaka"]`
- `src/api/client.js`:
  - 新增 `shouldFallback(error)` 函数: 503/5xx/网络错误触发 fallback，429/403/400 不触发
  - 新增 `withUpstreamFallback(fn)` 执行器: 按 candidates 顺序尝试
  - `buildHeaders(token, hostOverride)` 支持动态 host
  - 新增 `Transfer-Encoding: chunked` 请求头
- `src/utils/paths.js`: 新增 `upstreamJsonPath` 路径
- `scripts/build.js`: 打包时包含 upstream.json
- `src/constants/index.js`: 新增 `SHORT_COOLDOWN_THRESHOLD` 常量

**本地状态**: `src/config/config.js` 有大量本地修改 (+373 行)，`src/api/client.js` 有 6 个提交 (+701 行)。

**优化建议**:
- 🔑 **高价值优化** — upstream fallback 是重要的可靠性增强
- 实施步骤:
  1. 新建 `src/config/upstream.json` (直接采纳)
  2. 在 `src/utils/paths.js` 添加 `upstreamJsonPath`
  3. 在 `src/config/config.js` 中添加 upstream.json 加载逻辑（注意与本地已有配置合并）
  4. 在 `src/api/client.js` 中实现 `shouldFallback()` + `withUpstreamFallback()`
  5. 修改 `buildHeaders()` 支持 `hostOverride`
- ⚠️ 注意: 本地 client.js 已有大量修改，需要手动将 fallback 逻辑嵌入现有代码

---

### 组 F：TLS 请求器更新 `d427212`

**目标**: 编译支持 HTTP/2 的 TLS 指纹请求器。

**upstream 实现**:
- 更新三个二进制文件 (fingerprint_*)
- 新建 `src/bin/tls_config_h2.json` (119 行): HTTP/2 TLS 配置

**本地状态**: `src/requester.js` 有 3 个提交 (+227 行)，主要是代理池 407 禁用和日志增强。

**优化建议**:
- ✅ **直接采纳** 二进制文件和 h2 配置
- 参考 upstream 在 `requester.js` 中添加 brotli/deflate 解压支持（本地可能已有类似实现）
- 注意: upstream 的 requester.js diff 显示新增了 `decompressBrotli()` 和 `decompressDeflate()` 辅助函数

---

### 组 G：端点/域名调整 `e615c4c` + `49ef73f` + `3d21227` + `a050d26` + `47820ad`

**目标**: 调整请求端点，添加 sukaka 端点，恢复渠道回退顺序。

**upstream 最终状态** (upstream.json):
```json
"upstreamCandidates": ["production", "sandbox", "daily", "sukaka"]
```

**本地状态**: 无直接相关修改。

**优化建议**:
- ✅ **直接采纳** — 纯配置变更，通过 upstream.json 引入即可

---

### 组 H：CSS 拆分 `1c7eb7e`

**目标**: 将 3700 行的 `public/style.css` 拆分为 5 个模块文件。

**upstream 实现**:
```
public/css/base.css       (161 行) - 基础样式/变量
public/css/components.css (698 行) - 组件样式
public/css/layout.css     (273 行) - 布局
public/css/pages.css      (2349 行) - 页面特定样式
public/css/responsive.css (376 行) - 响应式
public/style.css          (仅保留少量全局样式)
```

**本地状态**: 本地 `public/style.css` 可能有样式修改。

**优化建议**:
- ✅ **建议采纳** — CSS 拆分是纯重构，提升可维护性
- 如果本地有样式修改，需要把修改分配到对应的模块文件中

---

### 组 I：模型列表更新 `3bfd09e`

**目标**: 添加 `claude-opus-4-7` / `claude-opus-4-7-thinking` 到模型列表。

**本地状态**: 本地已有 `claude-opus-4-6` 映射相关修改。

**优化建议**:
- ✅ **直接采纳** — 在 `DEFAULT_MODELS` 数组中添加新模型

---

### 组 J：请求头字段 `dec77ea`

**目标**: 为对话接口请求头添加一个字段。

**upstream 实现**: `src/api/client.js` 中 +22/-21 行，具体是添加了 `Transfer-Encoding: chunked`。

**优化建议**:
- ✅ **直接采纳** — 在 `buildHeaders()` 中添加 `'Transfer-Encoding': 'chunked'`

---

## 二、本地大幅修改的文件分析

### 本地独有功能（upstream 没有的）

| 文件 | 本地提交数 | 本地独有功能 |
|------|-----------|-------------|
| `src/routes/admin.js` | **34** (+2752 行) | 代理池测试、407禁用池、403账号管理、OAuth API、批量操作、GeminiCLI 配额 |
| `src/auth/token_manager.js` | **11** (+1230 行) | 额度感知轮询、预热、随机起始、启动错误记录 |
| `src/api/client.js` | **6** (+701 行) | 前缀公共模型路由、代理池407禁用 |
| `src/auth/oauth_manager.js` | — (+399 行) | 批量 OAuth URL、回调 URL、密码认证 |
| `src/config/config.js` | — (+373 行) | 全局代理模式、大量配置项 |
| `src/utils/requesterManager.js` | — (+350 行) | 统一请求器（与 upstream 同名但不同实现） |
| `src/requester.js` | **3** (+227 行) | 代理池407识别、请求日志增强 |
| `src/server/stream.js` | **1** (+198 行) | GeminiCLI 额度分组 |

---

## 三、优化实施优先级

### 🥇 第一优先级（高价值 + 低风险）

| # | 优化项 | 来源 | 预计工作量 | 价值 |
|---|--------|------|-----------|------|
| 1 | 新建 `upstream.json` + 端点配置外置 | 组 E+G | 0.5h | 配置与代码分离，易维护 |
| 2 | TLS 二进制更新 + h2 配置 + brotli/deflate 解压 | 组 F | 0.5h | 支持 HTTP/2，解压更完善 |
| 3 | 模型列表更新 (claude-opus-4-7) | 组 I | 5min | 功能补全 |
| 4 | `Transfer-Encoding: chunked` 请求头 | 组 J | 5min | 兼容性 |

### 🥈 第二优先级（中等价值 + 中等风险）

| # | 优化项 | 来源 | 预计工作量 | 价值 |
|---|--------|------|-----------|------|
| 5 | 三档冷却策略 + tokenQuotaHelper | 组 C | 2h | 更智能的 429 处理 |
| 6 | 上游 baseURL Fallback | 组 E | 3h | 可靠性大幅提升 |
| 7 | 积分/订阅功能 | 组 D | 4h | 新功能 |
| 8 | CSS 拆分 | 组 H | 1h | 可维护性 |

### 🥉 第三优先级（高价值但高风险）

| # | 优化项 | 来源 | 预计工作量 | 价值 |
|---|--------|------|-----------|------|
| 9 | Token 管理器解耦重构 | 组 B | 8-12h | 架构清晰度 |
| 10 | RequesterManager 二进制探测 | 组 A | 1h | 启动可靠性 |

---

## 四、逐项实施指南

### 4.1 新建 upstream.json（第 1 优先级）

```bash
# 直接从 upstream 提取文件
git show upstream/main:src/config/upstream.json > src/config/upstream.json
```

然后在 `src/config/config.js` 中添加加载逻辑：

```javascript
// 在现有 config.json 加载之后添加:
import { readFileSync, existsSync } from 'fs';

// 加载 upstream.json（上游协议配置）
let upstreamConfig = {};
const upstreamJsonPath = path.join(__dirname, 'upstream.json');
if (existsSync(upstreamJsonPath)) {
  try {
    upstreamConfig = JSON.parse(readFileSync(upstreamJsonPath, 'utf8'));
  } catch (e) {
    log.warn(`加载 upstream.json 失败: ${e.message}`);
  }
}
```

在 `getActiveApiConfig()` 中优先使用 upstream.json 的端点配置。

---

### 4.2 TLS 更新 + 解压增强（第 1 优先级）

```bash
# 从 upstream 提取更新的二进制和配置
git checkout upstream/main -- src/bin/fingerprint_linux_amd64
git checkout upstream/main -- src/bin/fingerprint_windows_amd64.exe
git checkout upstream/main -- src/bin/fingerprint_android_arm64
git checkout upstream/main -- src/bin/tls_config_h2.json
```

在 `src/requester.js` 中添加（如果本地还没有）：

```javascript
// brotli 解压
function decompressBrotli(buffer) {
  return new Promise((resolve, reject) => {
    zlib.brotliDecompress(buffer, (err, result) => {
      if (err) reject(err);
      else resolve(result);
    });
  });
}

// deflate 解压
function decompressDeflate(buffer) {
  return new Promise((resolve, reject) => {
    zlib.inflate(buffer, (err, result) => {
      if (err) {
        zlib.inflateRaw(buffer, (err2, result2) => {
          if (err2) reject(err);
          else resolve(result2);
        });
      } else {
        resolve(result);
      }
    });
  });
}
```

---

### 4.3 三档冷却策略（第 2 优先级）

在 `src/constants/index.js` 添加:
```javascript
export const SHORT_COOLDOWN_THRESHOLD = 5000; // 5秒
```

新建 `src/utils/tokenQuotaHelper.js`:
```bash
git show upstream/main:src/utils/tokenQuotaHelper.js > src/utils/tokenQuotaHelper.js
```

修改 `src/server/stream.js` 中的 `with429Retry`:
- 添加 `shouldUseCredits` 参数
- 实现三档: 长冷却(≥1h) → 短冷却(≥5s) → 即时重试
- 长冷却时检查是否所有模型组都被禁用

修改三个 handler 的回调签名:
```javascript
// 之前:
() => generateAssistantResponse(requestBody, token, callback)
// 之后:
(attempt, shouldUseCredits) => {
  const actualBody = shouldUseCredits 
    ? { ...requestBody, enabledCreditTypes: ["GOOGLE_ONE_AI"] }
    : requestBody;
  return generateAssistantResponse(actualBody, token, callback);
}
```

---

### 4.4 上游 Fallback（第 2 优先级）

在 `src/api/client.js` 中添加:

```javascript
/**
 * 判断错误是否应触发 baseURL fallback
 */
function shouldFallback(error) {
  if (error?._skipFallback) return false;
  const status = getUpstreamStatus(error);
  if (status === 429 || status === 403 || status === 400) return false;
  if (status >= 500) return true;
  const networkCodes = [
    'ECONNREFUSED', 'ECONNRESET', 'ETIMEDOUT', 'ENOTFOUND',
    'EAI_AGAIN', 'EPIPE', 'UND_ERR_CONNECT_TIMEOUT'
  ];
  const code = error?.code || error?.cause?.code;
  if (code && networkCodes.includes(code)) return true;
  if (error?.message?.includes('timeout')) return true;
  return false;
}

/**
 * 带上游 baseURL fallback 的执行器
 */
async function withUpstreamFallback(fn) {
  const candidates = config.api.upstreamCandidates || ['production'];
  for (let i = 0; i < candidates.length; i++) {
    try {
      return await fn(candidates[i]);
    } catch (error) {
      if (i < candidates.length - 1 && shouldFallback(error)) {
        logger.warn(`[Fallback] ${candidates[i]} 失败，切换到 ${candidates[i+1]}`);
        continue;
      }
      throw error;
    }
  }
}
```

---

### 4.5 Token 管理器解耦参考（第 3 优先级）

这是最大的工程，建议分步进行：

**Step 1**: 先提取 `token_rotation_strategy.js`
```bash
git show upstream/main:src/auth/token_rotation_strategy.js > src/auth/token_rotation_strategy.js
```
将你的额度感知轮询逻辑迁移进去。

**Step 2**: 提取 `token_pool.js`
```bash
git show upstream/main:src/auth/token_pool.js > src/auth/token_pool.js
```
将 token 增删查改逻辑迁移。

**Step 3**: 提取 `token_validator.js` 和 `token_lifecycle_manager.js`

**Step 4**: 重构 `token_manager.js` 为组合模式

⚠️ **注意**: 不要直接用 upstream 版本覆盖，因为你的功能增强更丰富。应该是把你的代码按 upstream 的架构拆分。

---

### 4.6 RequesterManager 二进制探测（第 3 优先级）

在本地 `src/utils/requesterManager.js` 中添加 upstream 的 `_probeBinary()` 方法:

```javascript
/**
 * 主动 spawn 二进制文件做可执行性探测
 */
_probeBinary(binaryPath) {
  return new Promise((resolve, reject) => {
    const proc = spawn(binaryPath);
    proc.on('error', (err) => {
      reject(new Error(`二进制文件无法执行: ${err.message}`));
    });
    proc.on('spawn', () => {
      proc.kill();
      resolve();
    });
    proc.stdout.once('data', () => {
      proc.kill();
      resolve();
    });
  });
}
```

---

## 五、不建议采纳的变更

| upstream 变更 | 原因 |
|--------------|------|
| 直接替换 `token_manager.js` | 本地功能增强更多，直接替换会丢失功能 |
| 直接替换 `requesterManager.js` | 本地版本有更精细的签名检测和热重载 |
| 直接替换 `routes/admin.js` | 本地有 2752 行增强（代理测试、403管理等），upstream 只改了 185 行 |

---

## 六、快速执行命令参考

```bash
# === 第 1 优先级：低风险直接采纳 ===

# 1. 提取 upstream.json
git show upstream/main:src/config/upstream.json > src/config/upstream.json

# 2. 更新 TLS 二进制
git checkout upstream/main -- src/bin/fingerprint_linux_amd64
git checkout upstream/main -- src/bin/fingerprint_windows_amd64.exe  
git checkout upstream/main -- src/bin/fingerprint_android_arm64
git checkout upstream/main -- src/bin/tls_config_h2.json

# 3. 提取 tokenQuotaHelper
git show upstream/main:src/utils/tokenQuotaHelper.js > src/utils/tokenQuotaHelper.js

# 4. 提取测试文件（参考）
git show upstream/main:test/test-quota-exhausted-cascade.js > test/test-quota-exhausted-cascade.js
git show upstream/main:test/test-token-rotation-polling.js > test/test-token-rotation-polling.js
git show upstream/main:test/test-credits-extraction.js > test/test-credits-extraction.js
git show upstream/main:test/mock-accounts.json > test/mock-accounts.json

# === 查看 upstream 某个文件的完整内容（参考用）===
git show upstream/main:src/auth/token_pool.js
git show upstream/main:src/auth/token_rotation_strategy.js
git show upstream/main:src/auth/token_validator.js
git show upstream/main:src/auth/token_lifecycle_manager.js
git show upstream/main:src/auth/project_id_fetcher.js
```

---

## 七、从 `1c7eb7e` 开始的 upstream 提交依赖链与模块演进

> 说明：这里按 **提交顺序 + 前置关系 + 改动文件 + 模块归属** 整理，重点不是“能不能 merge”，而是“应该把 upstream 哪一条演进主线吸收到本地”。

### 7.1 总体主线

从 `1c7eb7e` 往后，upstream 主要分成 5 条主线：

1. **样式拆分主线**
   `1c7eb7e` → `3349732` → `aa2add8`
2. **Token 解耦主线**
   `92e44ba` → `2d4071b` / `b332693` / `24a8f99` / `0da6c36` / `271d630` / `d6c9e8d` / `2e9101c`
3. **积分使用主线**
   `2d4071b` + `b332693` + `3349732` → `906eb00` → `f78f1f1`
4. **上游端点配置主线**
   `aa2add8` → `49ef73f` → `3d21227` → `a050d26` → `47820ad`
5. **请求器 / 客户端主线**
   真正底座其实是 `aa29d82`，其后在 [`src/api/client.js`](src/api/client.js) 上继续叠加 `dec77ea` / `252a0bb` / `906eb00` / `aa2add8` / `3bfd09e`

### 7.2 提交依赖图（简化）

```text
aa29d82  请求器初始化重构
   └─ dec77ea  请求头字段调整
   └─ 252a0bb  修复禁用方法调用
   └─ 906eb00  积分真正参与请求
   └─ aa2add8  upstream 配置外置 + token routing 加强
   └─ 3bfd09e  模型列表更新

1c7eb7e  CSS 拆分
   └─ 3349732  alwaysUseCredits 配置 + 样式优化
      └─ f78f1f1  前端可调积分策略
      └─ aa2add8  继续整合前端样式与展示

92e44ba  token 管理器解耦
   └─ 2d4071b  token 禁用/冷却逻辑重构
      └─ 252a0bb  修复重构后遗漏调用
      └─ 906eb00  将积分使用接入 handler / stream
   └─ b332693  明确订阅获取逻辑
      └─ 0da6c36  前端追加订阅和积分显示
         └─ 271d630  前端刷新时同步刷新订阅和额度
      └─ d6c9e8d  再次微调订阅获取逻辑
   └─ 24a8f99  清理额度相关旧调用
   └─ 2e9101c  修正免费账号无积分的完整性判定

aa2add8  upstream.json 引入 + fallback 基础
   └─ 49ef73f  候选域名固定到生产域
      └─ 3d21227  恢复 daily
         └─ a050d26  添加 sukaka
            └─ 47820ad  最终回退顺序 production-sandbox-daily-sukaka
```

### 7.3 逐提交分析

#### `1c7eb7e` — 切割 css 文件，分为数个模块
- **前置**：无强依赖；它是后续样式改动的底座
- **后续依赖它**：`3349732`、`aa2add8`
- **改动文件**：
  - [`public/css/base.css`](public/css/base.css)
  - [`public/css/components.css`](public/css/components.css)
  - [`public/css/layout.css`](public/css/layout.css)
  - [`public/css/pages.css`](public/css/pages.css)
  - [`public/css/responsive.css`](public/css/responsive.css)
  - [`public/style.css`](public/style.css)
- **模块归属**：前端样式层、样式模块化、页面/组件样式分层
- **定位**：纯架构型样式重构，是前端样式后续演进的起点

#### `92e44ba` — 重构解耦 token 管理器
- **前置**：独立主线起点
- **后续依赖它**：`2d4071b`、`b332693`、`24a8f99`、`0da6c36`、`271d630`、`d6c9e8d`、`2e9101c`
- **改动文件**：
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
  - [`src/auth/token_lifecycle_manager.js`](src/auth/token_lifecycle_manager.js)
  - [`src/auth/token_manager.js`](src/auth/token_manager.js)
  - [`src/auth/token_pool.js`](src/auth/token_pool.js)
  - [`src/auth/token_rotation_strategy.js`](src/auth/token_rotation_strategy.js)
  - [`src/auth/token_validator.js`](src/auth/token_validator.js)
  - [`src/routes/admin.js`](src/routes/admin.js)
  - [`src/routes/sd.js`](src/routes/sd.js)
  - [`test/mock-accounts.json`](test/mock-accounts.json)
  - [`test/test-token-rotation-polling.js`](test/test-token-rotation-polling.js)
- **模块归属**：token 核心架构、token 池/轮询/生命周期、project id 获取、token 验证、admin 路由接线、测试补齐
- **定位**：整个 token 线的总前置提交

#### `2d4071b` — 重构 token 禁用相关逻辑
- **前置**：`92e44ba`
- **后续依赖它**：`252a0bb`、`906eb00`
- **改动文件**：
  - [`src/server/handlers/claude.js`](src/server/handlers/claude.js)
  - [`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)
  - [`src/server/handlers/openai.js`](src/server/handlers/openai.js)
  - [`src/server/stream.js`](src/server/stream.js)
  - [`src/utils/tokenQuotaHelper.js`](src/utils/tokenQuotaHelper.js)
  - [`test/test-quota-exhausted-cascade.js`](test/test-quota-exhausted-cascade.js)
- **模块归属**：429/503 重试与冷却、token 配额耗尽判定、handler/stream 联动、quota 辅助逻辑
- **定位**：把 token 冷却/禁用逻辑从管理器内部扩展到请求执行链路

#### `dec77ea` — 为对话接口请求头添加一个字段
- **前置**：更适合连同 `aa29d82` 一起看
- **改动文件**：
  - [`src/api/client.js`](src/api/client.js)
- **模块归属**：API 客户端、请求头构造、上游兼容性修正
- **定位**：小型协议修正提交，不是架构提交

#### `252a0bb` — 修复重构后未更新调用禁用方法引起的未定义问题
- **前置**：`2d4071b`
- **改动文件**：
  - [`src/api/client.js`](src/api/client.js)
- **模块归属**：API 客户端、token 禁用调用链修复
- **定位**：典型重构补丁，说明 `2d4071b` 改完后客户端仍有旧调用点

#### `b332693` — 明确获取订阅的逻辑
- **前置**：`92e44ba`
- **后续依赖它**：`0da6c36`、`271d630`、`d6c9e8d`，间接支撑 `906eb00`
- **改动文件**：
  - [`src/auth/oauth_manager.js`](src/auth/oauth_manager.js)
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
  - [`src/auth/token_lifecycle_manager.js`](src/auth/token_lifecycle_manager.js)
- **模块归属**：OAuth / project id / subscription 链路、token 生命周期与订阅同步
- **定位**：订阅/积分数据链路重构起点

#### `24a8f99` — 额度相关去除一些不存在的调用
- **前置**：`92e44ba`
- **改动文件**：
  - [`src/auth/token_manager.js`](src/auth/token_manager.js)
- **模块归属**：token manager 清理、额度相关旧接口清理
- **定位**：重构清尾，不是新能力

#### `0da6c36` — 前端追加订阅和积分显示
- **前置**：`b332693`
- **后续依赖它**：`271d630`
- **改动文件**：
  - [`public/js/tokens.js`](public/js/tokens.js)
  - [`src/auth/oauth_manager.js`](src/auth/oauth_manager.js)
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
  - [`src/auth/token_manager.js`](src/auth/token_manager.js)
  - [`test/test-credits-extraction.js`](test/test-credits-extraction.js)
- **模块归属**：前端 token 列表展示、订阅/积分提取、OAuth 到 token 数据贯通
- **定位**：第一次把订阅/积分链路展示到前端

#### `271d630` — 前端刷新也会刷新订阅和额度
- **前置**：`0da6c36`
- **改动文件**：
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
  - [`src/routes/admin.js`](src/routes/admin.js)
- **模块归属**：admin 刷新接口、project id / subscription 刷新
- **定位**：把前端刷新动作扩展为同步刷新订阅/额度

#### `3349732` — 添加配置总是使用积分和优化前端样式
- **前置**：样式上依赖 `1c7eb7e`；功能上为 `906eb00`、`f78f1f1` 做铺垫
- **后续依赖它**：`906eb00`、`f78f1f1`、`aa2add8`
- **改动文件**：
  - [`config.json.example`](config.json.example)
  - [`public/css/components.css`](public/css/components.css)
  - [`src/config/config.js`](src/config/config.js)
- **模块归属**：配置项新增、样式增强、积分策略开关底座
- **定位**：先放入“总是使用积分”这一配置概念，再调整一轮样式

#### `906eb00` — 添加积分的使用功能
- **前置**：主要依赖 `2d4071b`、`b332693`、`3349732`，间接承接 `0da6c36`、`271d630`
- **后续依赖它**：`f78f1f1`、`2e9101c`、`aa2add8`
- **改动文件**：
  - [`src/api/client.js`](src/api/client.js)
  - [`src/server/handlers/claude.js`](src/server/handlers/claude.js)
  - [`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)
  - [`src/server/handlers/openai.js`](src/server/handlers/openai.js)
  - [`src/server/stream.js`](src/server/stream.js)
  - [`src/utils/converters/common.js`](src/utils/converters/common.js)
- **模块归属**：请求发送链路、三个模型 handler、stream 重试/积分注入、通用转换器
- **定位**：积分真正参与请求发送的落地提交

#### `e615c4c` — 更新 daily 为 prod 端点
- **前置**：无强依赖
- **后续关系**：后来被 `aa2add8` 引入的 [`src/config/upstream.json`](src/config/upstream.json) 体系吸收
- **改动文件**：
  - [`config.json.example`](config.json.example)
- **模块归属**：配置示例、端点默认值说明
- **定位**：配置层小调整，后续被新配置体系重组

#### `f78f1f1` — 支持前端调整使用积分的策略
- **前置**：`3349732` + `906eb00`
- **改动文件**：
  - [`public/index.html`](public/index.html)
  - [`public/js/config.js`](public/js/config.js)
- **模块归属**：前端配置页、积分策略 UI
- **定位**：前端把积分策略能力开放给用户操作

#### `d6c9e8d` — 修改部分订阅获取逻辑
- **前置**：`b332693`
- **改动文件**：
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
- **模块归属**：subscription 获取逻辑微调、project id fetcher 优化
- **定位**：对 `b332693` 的局部修整

#### `aa2add8` — split upstream config and harden token routing
- **前置**：综合依赖前面多条主线，尤其是 `aa29d82`、`1c7eb7e`、`92e44ba`、`906eb00`
- **后续依赖它**：`49ef73f`、`3d21227`、`a050d26`、`47820ad`
- **改动文件**：
  - [`.gitignore`](.gitignore)
  - [`Dockerfile.binary`](Dockerfile.binary)
  - [`config.json.example`](config.json.example)
  - [`public/css/components.css`](public/css/components.css)
  - [`public/css/pages.css`](public/css/pages.css)
  - [`public/css/responsive.css`](public/css/responsive.css)
  - [`public/js/quota.js`](public/js/quota.js)
  - [`public/js/tokens.js`](public/js/tokens.js)
  - [`public/js/utils.js`](public/js/utils.js)
  - [`scripts/build.js`](scripts/build.js)
  - [`src/api/client.js`](src/api/client.js)
  - [`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
  - [`src/auth/token_manager.js`](src/auth/token_manager.js)
  - [`src/auth/token_store.js`](src/auth/token_store.js)
  - [`src/auth/token_validator.js`](src/auth/token_validator.js)
  - [`src/config/config.js`](src/config/config.js)
  - [`src/config/upstream.json`](src/config/upstream.json)
  - [`src/constants/index.js`](src/constants/index.js)
  - [`src/routes/admin.js`](src/routes/admin.js)
  - [`src/server/stream.js`](src/server/stream.js)
  - [`src/utils/configReloader.js`](src/utils/configReloader.js)
  - [`src/utils/paths.js`](src/utils/paths.js)
- **模块归属**：upstream 端点配置外置、token routing 加强、前端 quota/tokens 展示、build 打包兼容、config 路径与热重载
- **定位**：后半段最重要的综合整合提交

#### `2e9101c` — 设定免费账号没有积分不算信息不全
- **前置**：依赖前面已建立的订阅/积分判定逻辑
- **改动文件**：
  - [`src/auth/token_manager.js`](src/auth/token_manager.js)
- **模块归属**：token 信息完整性判定、免费账号规则修正
- **定位**：业务规则修补提交

#### `49ef73f` — 候选域名设置成固定的生产域名
- **前置**：`aa2add8`
- **后续依赖它**：`3d21227`、`a050d26`、`47820ad`
- **改动文件**：
  - [`src/config/upstream.json`](src/config/upstream.json)
- **模块归属**：上游候选端点配置
- **定位**：`upstream.json` 引入后的第一次策略调整

#### `d427212` — 编译支持 http2 的 tls 请求器，添加了 h2 配置文件
- **前置**：与 `aa2add8` 无强依赖；但属于请求器链路底层增强
- **改动文件**：
  - [`src/bin/fingerprint_android_arm64`](src/bin/fingerprint_android_arm64)
  - [`src/bin/fingerprint_linux_amd64`](src/bin/fingerprint_linux_amd64)
  - [`src/bin/fingerprint_windows_amd64.exe`](src/bin/fingerprint_windows_amd64.exe)
  - [`src/bin/tls_config_h2.json`](src/bin/tls_config_h2.json)
- **模块归属**：TLS 指纹二进制、HTTP/2 配置
- **定位**：底层请求器能力升级提交

#### `3d21227` — 恢复 daily 请求端点
- **前置**：`49ef73f` / `aa2add8`
- **后续依赖它**：`a050d26`、`47820ad`
- **改动文件**：
  - [`src/config/upstream.json`](src/config/upstream.json)
- **模块归属**：上游候选端点调整
- **定位**：继续修正候选端点顺序和内容

#### `a050d26` — 添加 sukaka 端点
- **前置**：`aa2add8`、`3d21227`
- **后续依赖它**：`47820ad`
- **改动文件**：
  - [`src/config/upstream.json`](src/config/upstream.json)
- **模块归属**：新上游端点接入、fallback 候选扩展
- **定位**：在已有候选端点体系里增加新端点

#### `3bfd09e` — 更新模型列表
- **前置**：客户端链路已稳定，建议与 `aa29d82` / `aa2add8` 一起看
- **改动文件**：
  - [`src/api/client.js`](src/api/client.js)
- **模块归属**：模型列表、client 默认模型集
- **定位**：纯数据更新型提交

#### `47820ad` — 恢复渠道回退 production-sandbox-daily-sukaka
- **前置**：`aa2add8` → `49ef73f` → `3d21227` → `a050d26`
- **改动文件**：
  - [`src/config/upstream.json`](src/config/upstream.json)
- **模块归属**：fallback 顺序最终定稿
- **定位**：端点候选链的最终排序修正

### 7.4 最关键的前置-后续关系

#### A. Token 主线
- `92e44ba` 是总前置
- `2d4071b` 是冷却 / 禁用侧前置
- `b332693` 是订阅 / 积分数据链路前置
- `906eb00` 是积分真正进入请求执行链的落地点
- `aa2add8` 是 token + config + routing 的综合整合点

#### B. 上游端点主线
- `aa2add8` 是总前置
- `49ef73f` → `3d21227` → `a050d26` → `47820ad` 是连续调参链
- 这条线本质上都围绕 [`src/config/upstream.json`](src/config/upstream.json) 展开

#### C. 样式与前端开关主线
- `1c7eb7e` 是样式架构底座
- `3349732` 是“积分策略配置 + 样式增强”前置
- `f78f1f1` 才是前端控制入口

#### D. 请求器 / 客户端主线
- 真前置是 `aa29d82`
- `dec77ea` / `252a0bb` / `906eb00` / `aa2add8` / `3bfd09e` 都是在 [`src/api/client.js`](src/api/client.js) 基础上继续叠加

### 7.5 对本地优化最值得参考的顺序

如果是为了“参考 upstream 逐步优化本地代码”，建议不是按提交时间机械照搬，而是按模块顺序吸收：

1. **先看 token 架构线**
   [`src/auth/token_manager.js`](src/auth/token_manager.js)、[`src/auth/token_pool.js`](src/auth/token_pool.js)、[`src/auth/token_lifecycle_manager.js`](src/auth/token_lifecycle_manager.js)、[`src/auth/token_rotation_strategy.js`](src/auth/token_rotation_strategy.js)、[`src/auth/project_id_fetcher.js`](src/auth/project_id_fetcher.js)
2. **再看积分使用线**
   [`src/server/stream.js`](src/server/stream.js)、[`src/server/handlers/claude.js`](src/server/handlers/claude.js)、[`src/server/handlers/gemini.js`](src/server/handlers/gemini.js)、[`src/server/handlers/openai.js`](src/server/handlers/openai.js)、[`src/api/client.js`](src/api/client.js)
3. **然后看端点配置线**
   [`src/config/config.js`](src/config/config.js)、[`src/config/upstream.json`](src/config/upstream.json)、[`src/utils/paths.js`](src/utils/paths.js)
4. **最后看前端展示线**
   [`public/js/tokens.js`](public/js/tokens.js)、[`public/js/quota.js`](public/js/quota.js)、[`public/js/config.js`](public/js/config.js)、[`public/index.html`](public/index.html)

---

## 八、总结

| 策略 | 文件数 | 工作量 | 风险 |
|------|--------|--------|------|
| 直接采纳（新文件/配置） | 8 | 1h | 低 |
| 手动移植功能点 | 6 | 6-8h | 中 |
| 参考架构重构 | 3 | 8-12h | 高 |
| **不采纳**（保留本地版本） | 3 | 0 | 无 |

**核心原则**: 不做纯粹合并，而是以本地代码为主体，参考 upstream 的优秀设计（fallback、三档冷却、解耦架构）进行渐进式优化。
